export function LoadingGrid({ count = 8 }: { count?: number }) { return <div className="token-grid" aria-label="Loading tokens" aria-busy="true">{Array.from({ length: count }, (_, index) => <div className="token-card token-card-skeleton" key={index}><span className="skeleton skeleton-avatar" /><span className="skeleton skeleton-line wide" /><span className="skeleton skeleton-line" /><span className="skeleton skeleton-block" /></div>)}</div>; }
export function StatePanel({ title, message, action }: { title: string; message: string; action?: React.ReactNode }) { return <div className="notice"><b>{title}</b><span>{message}</span>{action}</div>; }
